---
title: "08 Mixed Mode Switching"
---

## Goal

Demonstrate mode switching within a single program.

## Run

```bash
python3 bindings/python/get_started/courses/08-mode-mixed-switch.py
```

## Key Points

- Explicitly call `ensure_mode(...)` on every switch
- Send the command for the new mode immediately after switching
